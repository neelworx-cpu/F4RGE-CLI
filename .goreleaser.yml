# yaml-language-server: $schema=https://goreleaser.com/static/schema-pro.json
# vim: set ts=2 sw=2 tw=0 fo=jcroql
version: 2

project_name: 4rged

metadata:
  mod_timestamp: "{{ .CommitTimestamp }}"
  license: "FSL-1.1-MIT"
  homepage: "https://4rged.app/cli"
  description: "A powerful terminal-based AI assistant for developers, providing intelligent coding assistance directly in your terminal."
  full_description: "A powerful terminal-based AI assistant for developers, providing intelligent coding assistance directly in your terminal."
  maintainers:
    - "Neel Worx <neel@4rged.app>"

nightly:
  publish_release: true
  keep_single_release: true
  version_template: "{{ incminor .Version }}-nightly"

snapshot:
  version_template: "0.0.0-{{ .Timestamp }}"

before:
  hooks:
    - go mod tidy
    - rm -rf completions
    - mkdir completions
    - rm -rf manpages
    - mkdir manpages
    - sh -c 'go run . completion bash >./completions/4rged.bash'
    - sh -c 'go run . completion zsh >./completions/4rged.zsh'
    - sh -c 'go run . completion fish >./completions/4rged.fish'
    - sh -c 'go run . man | gzip -c >./manpages/4rged.1.gz'

# gomod:
#   proxy: true

builds:
  - env:
      - CGO_ENABLED=0
      - GOEXPERIMENT=greenteagc
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w -X github.com/neelworx-cpu/F4RGE-CLI/internal/version.Version={{.Version}}
    binary: 4rged
    flags:
      - -trimpath

archives:
  - name_template: >-
      4rged_
      {{- .Version }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- with .Arm}}v{{ . }}{{ end }}
    wrap_in_directory: true
    files:
      - README*
      - LICENSE*
      - manpages/*
      - completions/*
    format_overrides:
      - goos: windows
        formats: [zip]

checksum:
  name_template: "checksums.txt"

aur_sources:
  - private_key: "{{ .Env.AUR_KEY }}"
    disable: "{{ with .Prerelease }}true{{ end }}"
    git_url: "ssh://aur@aur.archlinux.org/4rged.git"
    commit_author:
      name: "4RGED"
      email: "release@4rged.app"
    makedepends:
      - go
      - git
    prepare: |-
      go mod download
    build: |-
      mkdir completions
      mkdir manpages
      export CGO_CPPFLAGS="${CPPFLAGS}"
      export CGO_CFLAGS="${CFLAGS}"
      export CGO_CXXFLAGS="${CXXFLAGS}"
      export CGO_LDFLAGS="${LDFLAGS}"
      export GOFLAGS="-buildmode=pie -trimpath -mod=readonly -modcacherw"
      go build -o 4rged -ldflags="-w -s -buildid='' -linkmode=external -X github.com/neelworx-cpu/F4RGE-CLI/internal/version.Version=v${pkgver}" .
      ./4rged completion bash >./completions/4rged.bash
      ./4rged completion zsh >./completions/4rged.zsh
      ./4rged completion fish >./completions/4rged.fish
      ./4rged man | gzip -c >./manpages/4rged.1.gz
    package: |-
      # bin
      install -Dm755 "./4rged" "${pkgdir}/usr/bin/4rged"
      # license
      mkdir -p "${pkgdir}/usr/share/licenses/4rged/"
      install -Dm644 ./LICENSE* "${pkgdir}/usr/share/licenses/4rged/"
      # completions
      mkdir -p "${pkgdir}/usr/share/bash-completion/completions/"
      mkdir -p "${pkgdir}/usr/share/zsh/site-functions/"
      mkdir -p "${pkgdir}/usr/share/fish/vendor_completions.d/"
      install -Dm644 "./completions/4rged.bash" "${pkgdir}/usr/share/bash-completion/completions/4rged"
      install -Dm644 "./completions/4rged.zsh" "${pkgdir}/usr/share/zsh/site-functions/_4rged"
      install -Dm644 "./completions/4rged.fish" "${pkgdir}/usr/share/fish/vendor_completions.d/4rged.fish"
      # man pages
      install -Dm644 "./manpages/4rged.1.gz" "${pkgdir}/usr/share/man/man1/4rged.1.gz"
      # readme
      mkdir -pv "${pkgdir}/usr/share/doc/4rged/"
      install -Dm644 README* "${pkgdir}/usr/share/doc/4rged/"

aurs:
  - private_key: "{{ .Env.AUR_KEY }}"
    disable: "{{ with .Prerelease }}true{{ end }}"
    git_url: "ssh://aur@aur.archlinux.org/4rged-bin.git"
    commit_author:
      name: "4RGED"
      email: "release@4rged.app"
    provides:
      - 4rged
    conflicts:
      - 4rged
    package: |-
      case "$CARCH" in
        aarch64)
          cd "${srcdir}/4rged_${pkgver}_Linux_arm64"
          ;;
        *)
          cd "${srcdir}/4rged_${pkgver}_Linux_${CARCH}"
          ;;
      esac
      # bin
      install -Dm755 "./4rged" "${pkgdir}/usr/bin/4rged"
      # license
      mkdir -p "${pkgdir}/usr/share/licenses/4rged/"
      install -Dm644 ./LICENSE* "${pkgdir}/usr/share/licenses/4rged/"
      # completions
      mkdir -p "${pkgdir}/usr/share/bash-completion/completions/"
      mkdir -p "${pkgdir}/usr/share/zsh/site-functions/"
      mkdir -p "${pkgdir}/usr/share/fish/vendor_completions.d/"
      install -Dm644 "./completions/4rged.bash" "${pkgdir}/usr/share/bash-completion/completions/4rged"
      install -Dm644 "./completions/4rged.zsh" "${pkgdir}/usr/share/zsh/site-functions/_4rged"
      install -Dm644 "./completions/4rged.fish" "${pkgdir}/usr/share/fish/vendor_completions.d/4rged.fish"
      # man pages
      install -Dm644 "./manpages/4rged.1.gz" "${pkgdir}/usr/share/man/man1/4rged.1.gz"
      # readme
      mkdir -pv "${pkgdir}/usr/share/doc/4rged/"
      install -Dm644 README* "${pkgdir}/usr/share/doc/4rged/"

furies:
  - disable: "{{ if (or .Prerelease .IsNightly) }}true{{ end }}"
    account: "{{ with .Env.FURY_TOKEN }}f4rge{{ else }}{{ end }}"
    secret_name: FURY_TOKEN

brews:
  - repository:
      owner: neelworx-cpu
      name: homebrew-f4rge
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    skip_upload: "{{ with .Prerelease }}true{{ end }}"
    commit_author:
      name: "4RGED"
      email: "release@4rged.app"
    goarm: 7
    extra_install: |-
      bash_completion.install "completions/{{ .ProjectName }}.bash" => "{{ .ProjectName }}"
      zsh_completion.install "completions/{{ .ProjectName }}.zsh" => "_{{ .ProjectName }}"
      fish_completion.install "completions/{{ .ProjectName }}.fish"
      man1.install "manpages/{{ .ProjectName }}.1.gz"

scoops:
  - repository:
      owner: neelworx-cpu
      name: scoop-f4rge
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    skip_upload: "{{ with .Prerelease }}true{{ end }}"
    commit_author:
      name: "4RGED"
      email: "release@4rged.app"

npms:
  - name: "@f4rge/4rged"
    repository: "git+https://github.com/neelworx-cpu/F4RGE-CLI.git"
    bugs: https://github.com/neelworx-cpu/F4RGE-CLI/issues
    access: public
    disable: "{{ with .Prerelease }}true{{ end }}"

nfpms:
  - formats:
      - apk
      - deb
      - rpm
      - archlinux
      - termux.deb
    file_name_template: "{{ .ConventionalFileName }}"
    contents:
      - src: ./completions/4rged.bash
        dst: '{{ if eq .Format "termux.deb" }}/data/data/com.termux/files/usr{{ end }}/etc/bash_completion.d/4rged'
      - src: ./completions/4rged.fish
        dst: '{{ if eq .Format "termux.deb" }}/data/data/com.termux/files{{ end }}/usr/share/fish/vendor_completions.d/4rged.fish'
      - src: ./completions/4rged.zsh
        dst: '{{ if eq .Format "termux.deb" }}/data/data/com.termux/files{{ end }}/usr/share/zsh/site-functions/_4rged'
      - src: ./manpages/4rged.1.gz
        dst: '{{ if eq .Format "termux.deb" }}/data/data/com.termux/files{{ end }}/usr/share/man/man1/4rged.1.gz'
    rpm:
      signature:
        key_file: '{{ if ne (index .Env "GPG_KEY_PATH") "" }}{{ .Env.GPG_KEY_PATH }}{{ else }}{{ end }}'
    deb:
      signature:
        key_file: '{{ if ne (index .Env "GPG_KEY_PATH") "" }}{{ .Env.GPG_KEY_PATH }}{{ else }}{{ end }}'

signs:
  - cmd: cosign
    signature: "${artifact}.sigstore.json"
    args:
      - sign-blob
      - "--bundle=${signature}"
      - "${artifact}"
      - "--yes"
    artifacts: checksum
    output: true

source:
  enabled: true

sboms:
  - artifacts: archive
  - id: source
    artifacts: source

nix:
  - repository:
      owner: "neelworx-cpu"
      name: nur-packages
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    commit_author:
      name: "4RGED"
      email: "release@4rged.app"
    license: fsl11Mit
    formatter: nixfmt
    skip_upload: "{{ with .Prerelease }}true{{ end }}"
    extra_install: |-
      installManPage ./manpages/4rged.1.gz
      installShellCompletion ./completions/*

winget:
  - publisher: F4RGE
    copyright: F4RGE
    publisher_url: https://4rged.app
    release_notes_url: "https://github.com/neelworx-cpu/F4RGE-CLI/releases/tag/{{.Tag}}"
    license_url: https://github.com/neelworx-cpu/F4RGE-CLI/blob/main/LICENSE.md
    skip_upload: "{{ with .Prerelease }}true{{ end }}"
    commit_author:
      name: "4RGED"
      email: "release@4rged.app"
    repository:
      owner: "neelworx-cpu"
      name: winget-pkgs
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
      branch: "4rged-{{.Version}}"
      pull_request:
        enabled: true
        draft: false
        check_boxes: true
        base:
          owner: microsoft
          name: winget-pkgs
          branch: master
        body: |
          /cc @andreynering

changelog:
  sort: asc
  disable: "{{ .IsNightly }}"
  use: github
  filters:
    exclude:
      - "^(build|ci): "
      - "^chore: auto-update generated files$"
      - "^chore: docs$"
      - "^chore: schema update$"
      - "^chore: schema$"
      - "^chore: typo$"
      - "^chore: update schema$"
      - "^chore: update$"
      - "^chore\\(legal\\): "
      - "^chore\\(deps\\): "
      - "^docs: update$"
      - "^test:"
      - "^test\\("
      - "^v\\d.*"
      - "merge conflict"
      - "merge conflict"
      - Merge branch
      - Merge pull request
      - Merge remote-tracking branch
      - go mod tidy
      - "^wip "
      - "^wip:"
  groups:
    - title: "Deps"
      regexp: "^.*\\(deps\\)*:+.*$"
      order: 300
    - title: "New!"
      regexp: "^.*feat[(\\w)]*:+.*$"
      order: 100
    - title: "Fixed"
      regexp: "^.*fix[(\\w)]*:+.*$"
      order: 200
    - title: "Docs"
      regexp: "^.*docs[(\\w)]*:+.*$"
      order: 400
    - title: "Other stuff"
      order: 9999

release:
  prerelease: auto
